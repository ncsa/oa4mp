This directory contains all of the deployment descriptors (web.xml) files
for OA4MP that are for the variosu authentication methods. Whichever
is current should be copied to the webapps/WEB-INF directory.

These are put into the standard OA4MP distribution.