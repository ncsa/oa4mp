#! /usr/bin/env -S qdl-run

/*
   Runs dune flow test with the authorization code flow. Mostly this is a regression test
   to show the authorization code flow works, although FNAL mostly uses the device flow.
*/
script_load('test#/local/fnal-dune-fork.qdl','uri');
